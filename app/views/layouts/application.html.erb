<!DOCTYPE html>
<html lang="en">
<html>
  <head>
    <title>Guillaume's Yelp</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Tangerine|neon|fire-animation">
    <link rel="stylesheet" type="text/css" href="/css/reset.css">  
    <link rel="stylesheet" type="text/css" href="/css/yelp.css">
  </head>

  <body>
    <header>
      <% if user_signed_in? %>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: 'header_button' %>
      <% else %>
        <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), class: 'header_button' %>
        <%= link_to "Sign in", new_user_session_path, class: 'header_button' %>
        <%= link_to "Sign up", new_user_registration_path, class: 'header_button' %>
      <% end %>
    </header>
    <% if !notice.nil? %>
      <div class="notice"> 
      <%= notice %>
      </div>
    <% end %>
    <%= yield %>    
  </body>
</html>
